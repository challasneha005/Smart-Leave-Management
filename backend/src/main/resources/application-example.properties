# Database configuration example

# Replace the placeholder values with your own settings.

# Never commit real passwords.

DB_URL=jdbc:mysql://localhost:3306/employee_leave_management?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=your_mysql_password

# Server port

PORT=8080
